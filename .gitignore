# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Test / tooling caches (never commit)
.pytest_cache/
.coverage
coverage.json
htmlcov/
.mypy_cache/
.ruff_cache/

# Node (jsdom smoke test installs this locally)
node_modules/

# Nova runtime state (never committed — lives on the HA instance)
nova_config.json
nova_persona.txt
patterns.db
*.jsonl
reasoning_cache.json
lockdown_state.json

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# Build artifacts
*.zip
build/
dist/
